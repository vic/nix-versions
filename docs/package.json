{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev .",
    "build": "vitepress build",
    "preview": "vitepress preview .",
    "push": "npm run build && rsync -avPzh -e ssh --delete .vitepress/dist/* $VITEPRESS"
  },
  "devDependencies": {
    "esbuild": ">=0.25.0",
    "vitepress": "^0.1.1"
  }
}
